AI Tool(s) Used: Gemini (Advanced Agentic Coding Assistant)

Scope of Usage:

1. Firebase Integration & Leaderboard
Location: assets/scripts/FirebaseManager.ts, assets/scripts/StartMenu.ts
Prompt & Response: "And implement the bonus feature: firebase and leaderboard" -> AI wrote FirebaseManager.ts using Firebase Web SDK v8 to handle Auth and Firestore requests. 
Refinement & Explanation: AI generated the dynamic HTML overlay for the login form to avoid configuring multiple Cocos nodes. The `saveHighScore` and `getLeaderboard` functions use Firestore to push and retrieve the top 10 scores based on user emails.

2. Procedural Level Generation & Dynamic Camera Fixes
Location: assets/scripts/LevelBuilder.ts, assets/scripts/CameraFollow.ts
Prompt & Response: "Can you make everything generated by level builder one z level below everything?", "Mario is now fallign on the roof of the hut"
Refinement & Explanation: AI assisted in setting `LevelContainer.zIndex = -1` to fix depth issues and updated the math for `groundY` calculation in the `FlagPole` landing sequence so Mario gracefully arcs into the hut instead of landing on the roof.

3. General Refactoring
Location: assets/scripts/Player.ts, assets/scripts/UIManager.ts
Prompt & Response: "Disable the next level on the final level finish, and submit the highscore"
Refinement & Explanation: AI added logic to recursively search for the `NextButton` in the UI hierarchy to hide it when `GameManager.instance.currentLevel` reaches the end of the unlocked levels array, ensuring players cannot proceed to an invalid level.

*Note: Please convert this file to AI_reference.pdf before submission as requested in the assignment spec.*
